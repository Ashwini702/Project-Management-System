*** Begin Patch
*** Update File: src/services/api.js
@@
-const api = axios.create({ baseURL: import.meta.env.VITE_API_URL || 'http://127.0.0.1:8000/api' });
+const api = axios.create({ baseURL: import.meta.env.VITE_API_URL || 'http://localhost:5000/api' });
@@
-  const token = localStorage.getItem('pms_access_token');
+  const token = localStorage.getItem('pms_token');
@@
-export const apiData = (response) => response.data?.results ?? response.data;
+export const apiData = (response) => response.data?.data ?? response.data;
*** Update File: src/services/authService.js
@@
-export const login = (credentials) => api.post('/auth/login/', credentials);
-export const register = (payload) => api.post('/auth/register/', payload);
-export const getProfile = () => api.get('/auth/profile/');
-export const updateProfile = (payload) => api.put('/auth/profile/update/', payload);
-export const logout = (refresh) => api.post('/auth/logout/', { refresh });
+export const login = (credentials) => api.post('/auth/login', credentials);
+export const me = () => api.get('/auth/me');
+export const logout = () => api.post('/auth/logout');
*** Update File: src/context/AuthContext.jsx
@@
-      localStorage.setItem('pms_access_token', data.access);
-      localStorage.setItem('pms_refresh_token', data.refresh);
-      setUser(data.user);
-      setAuthUser(data.user);
-      return { success: true, message: 'Login successful! Redirecting...', user: data.user };
+      localStorage.setItem('pms_token', data.data.token);
+      setUser(data.data.user);
+      setAuthUser(data.data.user);
+      return { success: true, message: data.message, user: data.data.user, dashboardPath: data.data.user.dashboardPath };
@@
-      const detail = error.response?.data?.non_field_errors?.[0] || error.response?.data?.detail;
+      const detail = error.response?.data?.message;
@@
-    const refresh = localStorage.getItem('pms_refresh_token');
-    if (refresh) authService.logout(refresh).catch(() => {});
-    localStorage.removeItem('pms_access_token');
-    localStorage.removeItem('pms_refresh_token');
+    authService.logout().catch(() => {});
+    localStorage.removeItem('pms_token');
*** Update File: src/components/auth/LoginForm.jsx
@@
-      const dashboardPath = getDashboardPathByRole(result.user.role);
+      const dashboardPath = result.dashboardPath || getDashboardPathByRole(result.user.role);
*** End Patch
